# Skills: Nous Specialist (nous-specialist)

## 1. Remote SSH Orchestration
* **Secure Key Management**: Utilizing SSH keys (`amx_hostinger_ed25519`) and configuring strict HostKeyChecking options.
* **VPS Shell Administration**: Executing command pipelines on remote staging/production servers.

## 2. Docker Compose & Deployment Systems
* **Container Orchestration**: Managing container lifetimes (`docker compose up`, `docker compose restart`).
* **Environment Configuration**: Modifying server env parameters (`.env`) and rebuilding targeted containers with zero downtime.

## 3. Database & Query Profiling
* **PostgreSQL Engine Operations**: Accessing database container consoles (`docker exec psql`), running queries, and validating foreign key states.
* **Migration Application**: Applying and auditing Drizzle migrations.

## 4. MCP Integration
* **Deliverable Registration**: Submitting files and preview URLs directly to the Paperclip Master Briefcase using the model context protocol.
