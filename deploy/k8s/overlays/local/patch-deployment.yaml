apiVersion: apps/v1
kind: Deployment
metadata:
  name: amx-app
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: amx-app
          imagePullPolicy: Never
      volumes:
        - name: paperclip-home
          persistentVolumeClaim:
            claimName: paperclip-home
