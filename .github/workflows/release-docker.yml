name: release-docker (retired)
on:
  workflow_dispatch:
permissions: {}
jobs:
  use-ci:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Images are published only by successful experimental CI. Use Deploy AMX VPS with its CI run ID."
          exit 1
