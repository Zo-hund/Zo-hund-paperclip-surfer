name: Deploy AMX Cluster (retired)
on:
  workflow_dispatch:
permissions: {}
jobs:
  use-vps:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::AMX-AIR-HUBS.CC now uses the governed Hostinger VPS pipeline. Use Deploy AMX VPS."
          exit 1
